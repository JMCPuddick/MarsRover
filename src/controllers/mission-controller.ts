import { Rover } from "../core";

/**
 * This class holds a collection of rover objects, and some metadata on the mission (number of rovers total, grid size, stuff like that)
 */
class MissionController {
    rovers: Array<Rover> = [];
}